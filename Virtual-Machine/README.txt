new a        //создает переменную a
cin a        //вводит a
cout a       //выводит a
set a 10     //a=10
setto a b    //a=b
add a b c    //a=b+c
minus a b c  //a=b-c
mul a b c    //a=b*c
del a b c    //a=b/c  (получаем float)
deln a b c   //a=b/c  (деление нацело)
ost a b c    //a=b%c  (остаток)
exit         //выход из программы
----------------------------------------
if a b jump     //if(a==b) 
true => идет на след строку
false => прыгает на след после jump
---------------------------------------
ifnu a 2 jump    //тоже самое, но a==2 (сравнение переменной и числа)
----------------------------------------
ifnot a b jump     //if(a!=b)
true => идет на след строку
false => прыгает на след после jump
---------------------------------------
ifnotnu a 2 jump    //тоже самое, но a!=2 (сравнение переменной и числа)
----------------------------------------
while 
ifnot a b jump
...
...
...
move while
jump
------
while (a!=b){
}
----------------------------------------